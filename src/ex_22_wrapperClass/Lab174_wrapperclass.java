package ex_22_wrapperClass;

public class Lab174_wrapperclass {
    int a  = 10;
    Integer a1 = 10;

    //Wrapper class is a class that encapsulates or wraps a primitive datatype into an object
    //These are same in nature but in wrapper classes we will get some in built functions
    //we have wrapper class of integer,character,boolean,short, long, double,Byte,Float
}
