package ex_01_JavaBasics;

public class Enum {

    // Class name can be ENUM as well. as enum is a keyword and Enum is a normal word.
    // And java is case sensitive.
    // So we can use Enum as identifier.

}
